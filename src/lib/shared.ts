export const appName = "Effect Codelab";
export const docsRoute = "/learn";

// Everything canonical, every absolute URL in meta, sitemap and JSON-LD
// derives from this one constant.
export const siteUrl = "https://effect.rangga.site";

export const siteDescription =
  "Short tracks on the Effect library for TypeScript. Every snippet is compiled by the build, so the code you read is code that runs.";

export const gitConfig = {
  user: "derangga",
  repo: "effect-codelab",
  branch: "main",
};
