# Everything under /assets/ carries a content hash in its filename, so a
# changed file is a new url and the old one can be cached forever. Without
# this, Cloudflare revalidates all 197 of them on every visit.
/assets/*
  Cache-Control: public, max-age=31536000, immutable
