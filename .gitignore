node_modules
.DS_Store
dist
dist-ssr
*.local
# The demo's base URL is a constant in products.ts now, so there is no .env to
# commit. Anything private would go in .env.local, which never leaves the disk.
.env
.env.local
.env.*.local
.nitro
.tanstack
.wrangler
.output
.vinxi
__unconfig*
.source
.cache
.vercel
todos.json
